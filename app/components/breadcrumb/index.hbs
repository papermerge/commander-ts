<ul>
    {{#each this.data.value.attributes.breadcrumb as |item|}}
        <Breadcrumb::Item
            @id={{get item 1}}
            @title={{get item 0}}
            @onClick={{@onClick}} />
    {{/each}}
</ul>